// Objeto Math
let resultado;
// PI
resultado = Math.PI;

// Redondea hacia abajo 2.4 = 2 y 2.5 = 3
resultado = Math.floor(2.8);
resultado = Math.floor(2.2);

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondea hacia abajo
resultado = Math.floor(2.9);

// Raiz Cuadrada
resultado = Math.sqrt(144);

// Valor absoluto (-500) ignora el negativo
resultado = Math.abs(-500);

// Potencia
resultado = Math.pow(3, 4);

// Minimo de un numero
resultado = Math.min(2, 3, 4, 5, 1, 3);

// Maximo de una serie de numeros
resultado = Math.max(2, 3, 2, 4, 5, 100);

//Numeros aleatoriom "No es tan bueno"
resultado = Math.random();

console.log(resultado);
