const producto = "Monitor 20 pulgadas";

// toUpperCase() permite convertir una cadena a mayusculas
console.log(producto.toUpperCase());

// toLowerCase() permite convertir una cadena a minusculas
console.log(producto.toLowerCase());

// Convertir un numero a cadena
const precio = 100;
console.log(precio.toString() + 100);
