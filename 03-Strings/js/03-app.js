const producto = "Monitor 20 pulgadas";
const precio = "20 USD";

// Concatenar Strings
console.log(producto.concat(" - ", precio));
console.log(producto + " " + precio);
console.log(producto, " ", precio);
console.log(`${producto} - ${precio}`);
